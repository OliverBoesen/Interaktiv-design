
// --------------------------- Timeline - Start ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det start-afsnit
  var section1900 = document.getElementById('Start');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="Start"]');
  
  // Tjek om start-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1900 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1900-afsnit
  var section1900 = document.getElementById('1900');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1900"]');
  
  // Tjek om 1900-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1910 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1910-afsnit
  var section1900 = document.getElementById('1910');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1910"]');
  
  // Tjek om 1910-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1920 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1920-afsnit
  var section1900 = document.getElementById('1920');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1920"]');
  
  // Tjek om 1920-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1930 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1930-afsnit
  var section1900 = document.getElementById('1930');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1930"]');
  
  // Tjek om 1930-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1940 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1940-afsnit
  var section1900 = document.getElementById('1940');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1940"]');
  
  // Tjek om 1940-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1950 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1950-afsnit
  var section1900 = document.getElementById('1950');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1950"]');
  
  // Tjek om 1950-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1960 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1960-afsnit
  var section1900 = document.getElementById('1960');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1960"]');
  
  // Tjek om 1960-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1970 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1970-afsnit
  var section1900 = document.getElementById('1970');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1970"]');
  
  // Tjek om 1970-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1980 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1980-afsnit
  var section1900 = document.getElementById('1980');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1980"]');
  
  // Tjek om 1980-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 1990 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 1990-afsnit
  var section1900 = document.getElementById('1990');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="1990"]');
  
  // Tjek om 1990-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 2000 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 2000-afsnit
  var section1900 = document.getElementById('2000');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="2000"]');
  
  // Tjek om 2000-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - 2010 ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det 2010-afsnit
  var section1900 = document.getElementById('2010');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="2010"]');
  
  // Tjek om 2010-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});

// --------------------------- Timeline - Nu ------------------------

// Lyt efter scrollhændelser
window.addEventListener('scroll', function() {
  // Find det nu-afsnit
  var section1900 = document.getElementById('nu');
  // Find det tilsvarende tekstelement på tidslinjen
  var text1900 = document.querySelector('.point-year[data-year="nu"]');
  
  // Tjek om nu-afsnittet er synligt på skærmen
  var sectionRect = section1900.getBoundingClientRect();
  if (sectionRect.top < window.innerHeight && sectionRect.bottom >= 0) {
      // Tilføj en klasse til det tilsvarende tekstelement på tidslinjen
      text1900.classList.add('active');
  } else {
      // Fjern klassen hvis afsnittet ikke er synligt
      text1900.classList.remove('active');
  }
});